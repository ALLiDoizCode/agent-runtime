# Test configuration for Connector B (linear topology integration test)
# Test topology: A → B → C
nodeId: connector-b
btpServerPort: PLACEHOLDER_PORT_B  # Will be replaced dynamically in test
logLevel: error

peers:
  - id: connector-c
    url: ws://localhost:PLACEHOLDER_PORT_C
    authToken: '{"peerId":"connector-b","secret":"secret-b"}'

routes:
  - prefix: g.connector-c
    nextHop: connector-c
