# Invalid: ILP address prefix contains uppercase
nodeId: test-connector
btpServerPort: 3000
peers:
  - id: peer-a
    url: ws://peer-a:3001
    authToken: secret-a
routes:
  - prefix: G.INVALID
    nextHop: peer-a
