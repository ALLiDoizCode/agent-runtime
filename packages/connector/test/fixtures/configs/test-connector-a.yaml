# Test configuration for Connector A (linear topology integration test)
# Test topology: A → B → C
nodeId: connector-a
btpServerPort: PLACEHOLDER_PORT_A  # Will be replaced dynamically in test
logLevel: error

peers:
  - id: connector-b
    url: ws://localhost:PLACEHOLDER_PORT_B
    authToken: '{"peerId":"connector-a","secret":"secret-a"}'

routes:
  - prefix: g.connector-b
    nextHop: connector-b

  - prefix: g.connector-c
    nextHop: connector-b
