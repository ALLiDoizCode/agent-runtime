# Invalid config - route prefix is number instead of string
nodeId: test-connector
btpServerPort: 3000
peers:
  - id: peer-a
    url: ws://peer:3001
    authToken: secret
routes:
  - prefix: 123
    nextHop: peer-a
