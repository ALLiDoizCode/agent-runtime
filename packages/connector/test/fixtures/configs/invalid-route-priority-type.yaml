# Invalid config - route priority is string instead of number
nodeId: test-connector
btpServerPort: 3000
peers:
  - id: peer-a
    url: ws://peer:3001
    authToken: secret
routes:
  - prefix: g.test
    nextHop: peer-a
    priority: "high"
