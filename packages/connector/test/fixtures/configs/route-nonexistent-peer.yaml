# Invalid: route references non-existent peer
nodeId: test-connector
btpServerPort: 3000
peers:
  - id: peer-a
    url: ws://peer-a:3001
    authToken: secret-a
routes:
  - prefix: g.test
    nextHop: unknown-peer
