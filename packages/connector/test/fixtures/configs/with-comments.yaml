# Valid configuration with extensive comments

# This is a test connector configuration
nodeId: test-connector  # Node identifier

# Server configuration
btpServerPort: 3000     # BTP server port

# Peer definitions
peers:
  # First peer - connector A
  - id: peer-a
    url: ws://peer-a:3001  # WebSocket URL
    authToken: secret-a     # Shared secret

# Routing configuration
routes:
  # Route to peer A
  - prefix: g.peera
    nextHop: peer-a
